{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    // Adding this for consistency and future-proofing, as other packages
    // might eventually reference 'core'.
    "composite": true
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"],
  "references": [
    { "path": "../genome" }
  ]
}
